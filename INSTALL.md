# دليل التثبيت والتشغيل (Makhzani) — للمبتدئين تمامًا

هذا الدليل يفترض أنك **لا تملك أي خبرة سابقة**. اتبع الخطوات بالترتيب. كل خطوة فيها أمر
جاهز للنسخ واللصق في الـ Terminal (نافذة الأوامر).

> **ملاحظة:** هذا الدليل سيصبح دقيقًا 100% بعد إنشاء هيكل مشروع Next.js فعليًا (Phase 0
> من خارطة الطريق في `docs/ARCHITECTURE.md`). حاليًا يشرح المسار العام والأدوات التي ستحتاجها،
> حتى تكون جاهزًا وتعرف ماذا يعني كل مصطلح قبل أن نبدأ التنفيذ.

---

## 1. الأدوات التي تحتاج تثبيتها على جهازك (مرة واحدة فقط)

### أ) Node.js
هذا هو "المحرك" الذي يشغّل المشروع.
1. اذهب إلى https://nodejs.org
2. حمّل النسخة الموصى بها (LTS)
3. ثبّتها بالضغط على "Next" حتى النهاية
4. للتأكد أنها اشتغلت، افتح PowerShell واكتب:

```bash
node -v
```

يجب أن يظهر رقم إصدار (مثل `v20.x.x`).

### ب) Git
لإدارة الكود ورفعه على GitHub.
1. حمّل من https://git-scm.com/downloads
2. ثبّته بالإعدادات الافتراضية

### ج) حسابات GitHub / Vercel / Supabase

لديك بالفعل حسابات على الثلاثة، فقط تأكد أنهم مربوطين ببعض (يُفضَّل الدخول لـ Vercel و
Supabase عبر GitHub مباشرة "Continue with GitHub" حتى يسهل ربط المشروع لاحقًا).

### د) الحصول على رابطي الاتصال من Supabase (خطوة ستحتاجها بالضبط عند بدء Phase 0)

Supabase (بخلاف Neon) يعطي **رابطين** يجب استخدامهما معًا مع Prisma:

1. من لوحة مشروعك في Supabase → **Project Settings → Database**
2. تحت **Connection string**:
   - اختر **"Connection pooling"** (Transaction mode, المنفذ عادة `6543`) → هذا يصبح `DATABASE_URL`
   - اختر **"Direct connection"** (المنفذ عادة `5432`) → هذا يصبح `DIRECT_URL`
3. احتفظ بالرابطين، ستحتاجهما في الخطوة التالية بالضبط

> **لماذا رابطان؟** رابط الـ Pooler هو الذي يستخدمه الموقع أثناء التشغيل الفعلي (يتحمّل عددًا
> كبيرًا من الاتصالات المتزامنة بكفاءة). الرابط المباشر يُستخدم فقط عند تعديل هيكل قاعدة
> البيانات (`prisma migrate`). هذا التفصيل موجود جاهزًا في `prisma/schema.prisma` ولا يحتاج
> منك أي إعداد إضافي غير نسخ الرابطين الصحيحين.

---

## 2. تحميل المشروع على جهازك

بعد رفع الكود على GitHub (سنقوم بذلك سويًا عند بدء التنفيذ)، لتحميله على أي جهاز:

```bash
git clone https://github.com/mwsfaty-afk/makhzani.git
cd makhzani
npm install
```

`npm install` يقوم بتحميل كل "المكتبات" التي يحتاجها المشروع ليعمل — قد يأخذ دقيقتين.

---

## 3. إعداد ملف البيئة (Environment Variables)

المشروع يحتاج ملف اسمه `.env` (لا يُرفع على GitHub أبدًا لأنه يحتوي أسرارًا) فيه:

```bash
DATABASE_URL="postgresql://...رابط Connection Pooling من Supabase..."
DIRECT_URL="postgresql://...رابط Direct Connection من Supabase..."
NEXTAUTH_SECRET="نص عشوائي طويل - يمكن توليده بالأمر التالي"
NEXTAUTH_URL="http://localhost:3000"
```

لتوليد `NEXTAUTH_SECRET` عشوائي وآمن:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

انسخ الناتج والصقه بدل النص بين علامتي التنصيص.

---

## 4. تجهيز قاعدة البيانات (أول مرة فقط، أو بعد أي تعديل على الجداول)

```bash
npx prisma generate
npx prisma migrate dev
npm run db:seed
```

الأمر الأول يجهّز "المترجم" بين الكود وقاعدة البيانات، والثاني ينشئ كل الجداول فعليًا على Supabase،
والثالث يملأ كتالوج الصلاحيات وخطة التجربة المجانية — بدونه لن تعمل صفحة "إنشاء حساب شركة".

---

## 5. تشغيل المشروع على جهازك (للتجربة قبل النشر)

```bash
npm run dev
```

بعدها افتح المتصفح على: http://localhost:3000

لإيقاف التشغيل: اضغط `Ctrl + C` داخل الـ Terminal.

---

## 6. النشر على الإنترنت (Vercel) — بدون أي أوامر تقنية معقدة

1. ادخل إلى https://vercel.com بعد تسجيل الدخول
2. اضغط **"Add New" → "Project"**
3. اختر المستودع (Repository) `stock-prog` من قائمة مستودعات GitHub الخاصة بك
4. في خطوة "Environment Variables"، أضف نفس المتغيرات الموجودة في ملف `.env` عندك
   (`DATABASE_URL`, `DIRECT_URL`, `NEXTAUTH_SECRET`, وسيُضاف `NEXTAUTH_URL` تلقائيًا برابط الموقع الحقيقي)
5. اضغط **Deploy**

بعد دقيقة أو اثنتين، سيعطيك Vercel رابطًا مباشرًا مثل `https://stock-prog.vercel.app` — هذا هو
موقعك المباشر على الإنترنت.

**من الآن فصاعدًا:** في كل مرة يتم فيها `git push` على الفرع الرئيسي، Vercel يبني وينشر
التحديث تلقائيًا خلال دقائق — لا حاجة لتكرار أي خطوة يدويًا.

---

## 7. الأوامر التي ستستخدمها بشكل متكرر (مرجع سريع)

| الأمر | ماذا يفعل |
|---|---|
| `npm run dev` | تشغيل المشروع على جهازك للتجربة |
| `npm run build` | التأكد أن المشروع يُبنى بدون أخطاء (قبل الرفع) |
| `npx prisma studio` | فتح واجهة مرئية لتصفح قاعدة البيانات مباشرة في المتصفح |
| `npx prisma migrate dev` | تطبيق أي تعديل جديد على هيكل قاعدة البيانات |
| `git add . && git commit -m "..." && git push` | رفع تعديلاتك — يشغّل نشرًا تلقائيًا على Vercel |

---

## 8. عند وجود مشكلة

- خطأ يتعلق بـ `DATABASE_URL` أو `DIRECT_URL`: تأكد أنك نسخت الرابط الصحيح من Supabase لكل متغير
  (Pooling لـ `DATABASE_URL`، Direct لـ `DIRECT_URL`) بدون مسافات إضافية، وأن كلمة المرور بداخل
  الرابط مطابقة لكلمة مرور قاعدة البيانات في مشروعك على Supabase.
- الموقع يعمل محليًا لكن ليس على Vercel: تأكد أن نفس متغيرات `.env` مضافة في إعدادات المشروع
  على Vercel (Settings → Environment Variables)، وليس فقط على جهازك.
- أي خطأ آخر: انسخ نص الخطأ كاملًا من الـ Terminal — سنحتاجه لتشخيص المشكلة بدقة.
